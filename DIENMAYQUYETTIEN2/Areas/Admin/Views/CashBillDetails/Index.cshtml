@model IEnumerable<DIENMAYQUYETTIEN2.Models.CashBillDetail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Admin_Layout.cshtml";
}

<h2 style="padding-left: 250px;font-size: 40px; color:pink;">List Products</h2>

<table class="table" style="width: 70%;" >
    <thead>
        <tr>
            <th>
                Quantity
            </th>
            <th>
                Product Name
            </th>
            <th>
                Sale Price
            </th>
            <th>
                Total
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Product.ProductName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SalePrice)
                </td>
                <td>
                    @(item.Quantity * item.SalePrice)
                </td>
                
            </tr>
        }
    </tbody>
</table>

<div>
    <div class="form-group">
        <label class="control-label col-md-2">Grand Total</label>
        <div class="col-md-10">
            <input id="GrandTotal2"  class="disabled" value="@Model.Sum(m => m.SalePrice * m.Quantity)" disabled /> VNĐ
        </div>
    </div>
</div>